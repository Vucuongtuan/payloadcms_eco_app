import { Product } from "@/payload-types";

interface GridCompProps {
  gap: number;
  data: Product[];
}
export default function GridComp(props: GridCompProps) {
  const { gap, data } = props;
  return <></>;
}
